<% team = @team || @team %>
<% context ||= team %>
<% collection ||= :properties %>
<% hide_actions ||= false %>
<% hide_back ||= false %>

<% with_attribute_settings object: property do %>
  <tr data-id="<%= property.id %>">
    <%= render "shared/tables/checkbox", object: property %>
    <td><%= render 'shared/attributes/text', attribute: :name, url: [:account, property] %></td>
    <td><%= render 'shared/attributes/text', attribute: :description %></td>
    <%# 🚅 super scaffolding will insert new fields above this line. %>
    <td><%= render 'shared/attributes/date_and_time', attribute: :created_at %></td>
    <td class="buttons">
      <% unless hide_actions %>
        <% if can? :edit, property %>
          <%= link_to t('.buttons.shorthand.edit'), [:edit, :account, property], class: 'button-secondary button-smaller' %>
        <% end %>
        <% if can? :destroy, property %>
          <%= button_to t('.buttons.shorthand.destroy'), [:account, property], method: :delete, data: { confirm: t('.buttons.confirmations.destroy', model_locales(property)) }, class: 'button-secondary button-smaller' %>
        <% end %>
        <%# 🚅 super scaffolding will insert new action model buttons above this line. %>
      <% end %>
    </td>
  </tr>
<% end %>
