<% bookings = bookings.accessible_by(current_ability) %>
<% property = @property %>
<% context ||= property %>
<% collection ||= :bookings %>
<% hide_actions ||= false %>
<% hide_back ||= false %>

<% pagy ||= nil %>
<% pagy, bookings = pagy(bookings, page_param: :bookings_page) unless pagy %>

<%= action_model_select_controller do %>
  <% cable_ready_updates_for context, collection do %>
    <%= render 'account/shared/box', pagy: pagy do |box| %>
      <% box.title t(".contexts.#{context.class.name.underscore}.header") %>
      <% box.description do %>
        <%= t(".contexts.#{context.class.name.underscore}.description#{"_empty" unless bookings.any?}") %>
        <%= render "shared/limits/index", model: bookings.model %>
      <% end %>

      <% box.table do %>
        <% if bookings.any? %>
          <table class="table">
            <thead>
              <tr>
                <%= render "shared/tables/select_all" %>
                <th><%= t('.fields.start_date_time.heading') %></th>
                <th><%= t('.fields.end_date_time.heading') %></th>
                <th><%= t('.fields.extra_info.heading') %></th>
                <th><%= t('.fields.access_info.heading') %></th>
                <%# 🚅 super scaffolding will insert new field headers above this line. %>
                <th><%= t('.fields.created_at.heading') %></th>
                <th class="text-right"></th>
              </tr>
            </thead>
            <tbody>
              <%= render partial: 'account/bookings/booking', collection: bookings %>
            </tbody>
          </table>
        <% end %>
      <% end %>

      <% box.actions do %>
        <% unless hide_actions %>
          <% if context == property %>
            <% if can? :create, Booking.new(property: property) %>
              <%= link_to t('.buttons.new'), [:new, :account, property, :booking], class: "#{first_button_primary(:booking)} new" %>
            <% end %>
          <% end %>

          <%# 🚅 super scaffolding will insert new targets one parent action model buttons above this line. %>
          <%# 🚅 super scaffolding will insert new bulk action model buttons above this line. %>
          <%= render "shared/bulk_action_select" if bookings.many? %>

          <% unless hide_back %>
            <%= link_to t('global.buttons.back'), [:account, context], class: "#{first_button_primary(:booking)} back" %>
          <% end %>
        <% end %>
      <% end %>

      <% box.raw_footer do %>
        <%# 🚅 super scaffolding will insert new action model index views above this line. %>
      <% end %>
    <% end %>
  <% end %>
<% end %>
